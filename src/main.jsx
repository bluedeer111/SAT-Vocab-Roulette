import React from "react";
import ReactDOM from "react-dom/client";
import SatWordRoulette from "./SatWordRoulette.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SatWordRoulette />
  </React.StrictMode>
);
